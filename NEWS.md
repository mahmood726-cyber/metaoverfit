# metaoverfit 0.1.1

* Fixed `plot_overfitting(type="bar")` double-multiplication bug when used
  with `check_overfitting()` output (values already in percentage scale).
* Added `tests/testthat.R` runner file (required by CRAN).
* Added URL and BugReports fields to DESCRIPTION.
* Added LICENSE file (GPL-3).

# metaoverfit 0.1.0

* Initial release: optimism-corrected R^2_het via CV, bootstrap CIs,
  overfitting diagnostics, and sample size guidance.
